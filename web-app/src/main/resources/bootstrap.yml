spring:
  application:
    name: web-app
  profiles:
#    active: @spring.profiles.active@
    active: dev
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
#      discovery:
#        enabled: true
#        serviceId: configserver
