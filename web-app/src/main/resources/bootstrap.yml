spring:
  application:
    name: web-app
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:8888
      enabled: true
#      discovery:
#        enabled: true
#        serviceId: configserver
